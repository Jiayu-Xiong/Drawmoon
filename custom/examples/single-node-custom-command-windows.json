{
  "name": "Single Node – Custom Command (Windows)",
  "description": "A simple workflow with one node that runs a cmd.exe echo command on Windows.",
  "workflow": {
    "nodes": [
      {
        "id": "node-1",
        "label": "Echo Test",
        "config": {
          "provider": "custom",
          "mode": "chat",
          "cwd": ".",
          "prompt": "hello from workflow node",
          "contextMode": "fresh",
          "customCommand": "cmd.exe",
          "customArgs": ["/c", "echo %AGENT_PROMPT%"],
          "timeoutMs": 10000,
          "maxIterations": 1,
          "allowFileWrites": false
        }
      }
    ],
    "edges": []
  },
  "expected_events": ["start", "cache", "stdout", "complete"],
  "expected_stdout_contains": "hello from workflow node",
  "notes": "Runs cmd.exe /c echo %AGENT_PROMPT% and should output the prompt text. Use this to verify the runtime works before testing more complex providers."
}
