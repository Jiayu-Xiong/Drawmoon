/** Browser-safe path segments relative to the `xy/` monorepo root. */

/** Custom workflow HTTP runtime (canonical integration code). */
export const BACKEND_OPENCODE_DIR = "backend/opencode"

/** Vendored upstream OpenCode monorepo root. */
export const VENDORED_OPENCODE_ROOT = "backend/opencode/vendor/opencode"

/** Vendored upstream OpenCode CLI package. */
export const VENDORED_OPENCODE_CLI_DIR = "backend/opencode/vendor/opencode/packages/opencode"

export const FRONTEND_DEV_PORT = 4322
